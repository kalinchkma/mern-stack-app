/**
 * ==================================
 * manage game styles
 * ==================================
 */
import {styled, Container, Box} from "@mui/material";


export const ManageGameWrapper = styled(Container)({

});

export const ManageGameContainer = styled(Box)({
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    width: "100%"
})


